public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# Include optional native binaries for all platforms in the lockfile.
# Required for Railway (Linux) when lockfile is generated on macOS.
node-linker=node-modules